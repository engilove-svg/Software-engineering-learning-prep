# function lets us write the logic once and reuse it

def add(a, b):
    return a + b
# a and b are parameters — placeholders that receive values when we call the function.

# return sends a value back to the place where the function was called.

def multiply(a,b):
    return a*b
result = multiply(6, 4)
print(result)
