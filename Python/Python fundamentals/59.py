# A tuple is collection of values, similar to a list.
# The main difference is  list can be changed but tuples cannot.

person = ("Loveleen", 23, "Canada")

# Indexing works the same way
 
print(person[0])   # Loveleen
print(person[1])   # 23
print(person[-1])  # Canada

